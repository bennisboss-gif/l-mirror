# =====================================================================
#  A-REMPLIR-secrets-LU.txt  --  le SEUL fichier a remplir pour enroler LU
# =====================================================================
#  1. Renomme ce fichier EXACTEMENT : A-REMPLIR-secrets-LU.txt
#  2. Pose-le sur le Bureau de LU (ou Telechargements, ou C:\ , ou C:\SocleNode :
#     le script le trouve tout seul).
#  3. Remplis la ligne ci-dessous, enregistre, puis colle la ligne COCA.
#  Le script SUPPRIME ce fichier une fois qu'il a servi. Ne le versionne jamais.
# =====================================================================

# -- OBLIGATOIRE -------------------------------------------------------
# La cle d'enrolement Tailscale, generee FRAICHE en console juste avant :
#   Tailscale (console) -> Settings -> Keys -> Generate auth key
#   -> Reusable : OUI   |   Ephemeral : NON   |   Tags : tag:socle
#   (l'ancienne cle du repo est REVOQUEE : il en faut une nouvelle)
# Colle-la apres le "=" (garde le prefixe tskey-auth-...) :

TailscaleAuthKey = tskey-auth-COLLE-TA-CLE-FRAICHE-ICI


# -- OPTIONNEL (laisse tel quel si tu ne sais pas) ---------------------
# Ne remplis ceci QUE si tu veux ALSO monter la passerelle multi-modeles
# (LiteLLM) sur LU tout de suite. Sinon laisse la ligne commentee : LU
# demarrera avec Ollama seul, ce qui suffit a son role de secours.
# Valeur = ArchivePassphrase (bootstrap-code/SECRETS-V1.md, section 6).
#
# ArchivePassphrase = colle-ici-seulement-si-tu-veux-litellm
